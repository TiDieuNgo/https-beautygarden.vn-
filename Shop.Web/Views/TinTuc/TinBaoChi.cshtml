@{
    ViewBag.Title = "Tin tức";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.content404 = "index, follow, noodp, noydir";
    ViewBag.canonical = "https://beautygarden.vn/bao-chi-noi-gi-ve-chung-toi.html";
}
@model Shop.Web.Model.TinTucModel
@using PagedList.Mvc;
@using Shop.Web.Core.Helper
<div class="columns container">
    <ol class="breadcrumb no-hide">
        <li><a href="https://beautygarden.vn/" title="Beautygarden">Trang chủ</a></li>
        <li class="active"><a href="">Báo chí nói gì về chúng tôi</a></li>
    </ol>
    <div class="row">
        <div class="col-md-9 col-md-push-3  col-main ">
            <h2 class="page-heading">
                <span class="page-heading-title2">Tin báo chí</span>
            </h2>
            <div class="sortPagiBar clearfix">
                <div class="bottom-pagination">
                    <nav>
                        <ul class="pagination">
                            @Html.PagedListPager(Model.TinTucs, page => Url.Action("TinBaoChi", new { page }))
                        </ul>
                    </nav>
                </div>
            </div>
            <ul class="blog-posts">
                @if (Model.TinTucs.Any())
                {
                    foreach (var tinTuc in Model.TinTucs)
                    {
                        <li class="post-item">
                            <article class="entry">
                                <div class="row">
                                    <div class="col-sm-5">
                                        <div class="entry-thumb image-hover2">
                                            <a href="@tinTuc.Link" target="_blank" title="@tinTuc.Name">
                                                <img src="@string.Format("/Upload/Files/{0}",tinTuc.Img)" alt="@tinTuc.Name">
                                            </a>
                                        </div>
                                    </div>
                                    <div class="col-sm-7">
                                        <div class="entry-ci">
                                            <h3 class="entry-title"><a href="@tinTuc.Link" target="_blank">@tinTuc.Name</a></h3>
                                            <div class="entry-meta-data">
                                                <span class="date"> @tinTuc.sDate.ToShortDateString()</span>
                                            </div>
                                            <div class="entry-excerpt">
                                                @Html.Raw(tinTuc.Note)
                                            </div>
                                            <div class="entry-more">
                                                <a href="@tinTuc.Link" target="_blank" title="xem tiếp">Xem tiếp</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </article>
                        </li>
                    }
                }
            </ul>
            <div class="sortPagiBar clearfix">
                <div class="bottom-pagination">
                    <nav>
                        <ul class="pagination">
                            @Html.PagedListPager(Model.TinTucs, page => Url.Action("TinBaoChi", new { page }))
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
        <div class="col-md-3 col-md-pull-9 col-sidebar">
            @Html.Action("DanhMucInDanhSachSp", "Menu")
            @Html.Action("TinHot", "TinTuc")
            @Html.Action("SanPhamMoiVeInChiTiet", "TinTuc")
        </div>
    </div>
</div>


